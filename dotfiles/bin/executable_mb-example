#!/usr/bin/env bash
set -euo pipefail

: "${ACCELERANDO_HOME:?ACCELERANDO_HOME must point at repo root}"
export DENO_NO_UPDATE_CHECK=1
cd "$ACCELERANDO_HOME/scripts"
exec deno task --quiet mb-example "$@"
